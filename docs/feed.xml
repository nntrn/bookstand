---
---
<?xml version="1.0" encoding="utf-8"?>
{%- if site.url -%}
  {%- assign url_base = site.url | append: site.baseurl %}
{%- else -%}
  {%- assign url_base = site.github.url %}
{%- endif %}
{%- assign githubcoverurl = 'https://raw.githubusercontent.com/'| append: site.repository | append: '/'| append: site.book.branch | append: '/' | append: site.book.dir %}
<feed xmlns="http://www.w3.org/2005/Atom">
  {%- if site.title %}
  <title type="html">{{ site.title | smartify | xml_escape }}</title>
  {%- elsif site.name %}
  <title type="html">{{ site.name | smartify | xml_escape }}</title>
  {%- endif %}
  {%- if site.description %}
  <subtitle>{{ site.description | xml_escape }}</subtitle>
  {%- endif %}
  <link href="{{ page.url | prepend: url_base }}" rel="self" />
  <link href="{{ url_base }}/" />
  <id>{{ url_base | xml_escape }}/</id>
  <updated>{{ site.time | date_to_xmlschema }}</updated>

  {%- if site.author %}
    <author>
      {%- if site.author.name %}
        <name>{{ site.author.name | xml_escape }}</name>
      {%- else %}
        <name>{{ site.author | xml_escape }}</name>
      {%- endif %}
      {%- if site.author.email %}
        <email>{{ site.author.email | xml_escape }}</email>
      {%- endif %}
      {%- if site.author.uri %}
        <uri>{{ site.author.uri | xml_escape }}</uri>
      {%- endif %}
    </author>
  {%- endif %}
  {% assign posts = site.annotations | sort: "date" | reverse %}
  {%- for post in posts limit: 25 %}
    <entry>
      <title type="html">{{ post.title | smartify | strip_html | replace: '\n', ' ' | strip | xml_escape }}</title>
      <link href="{{ post.url | prepend: url_base }}" rel="alternate" type="text/html" title="{{ post.title | xml_escape }}" />
      <published>{{ post.date | date_to_xmlschema }}</published>
      {%- if post.modified %}
      <updated>{{ post.modified | date_to_xmlschema }}</updated>
      {%- else %}
      <updated>{{ post.date | date_to_xmlschema }}</updated>
      {%- endif %}
      <id>{{ post.id | absolute_url | xml_escape }}</id>
      <content type="html" xml:base="{{ post.url | prepend: url_base | xml_escape }}"></content>
      {%- assign post_author = post.author | default: post.authors[0] | default: site.author %}
      {%- assign post_author = site.data.authors[post_author] | default: post_author %}
      {%- assign post_author_email = post_author.email | default: nil %}
      {%- assign post_author_uri = post_author.uri | default: nil %}
      {%- assign post_author_name = post_author.name | default: post_author %}
      {%- if post_author %}
        <author>
            <name>{{ post_author_name | xml_escape }}</name>
          {%- if post_author_email -%}
            <email>{{ post_author_email | xml_escape }}</email>
          {%- endif %}
          {%- if post_author_uri -%}
            <uri>{{ post_author_uri | xml_escape }}</uri>
          {%- endif %}
        </author>
      {%- endif %}
      {%- if post.category %}
        <category term="{{ post.category | xml_escape }}" />
      {%- endif %}
      {%- for tag in post.tags %}
        <category term="{{ tag | xml_escape }}" />
      {%- endfor %}
      {%- if post.excerpt and post.excerpt != empty %}
        <summary type="html">{{ post.description | strip_html | normalize_whitespace | xml_escape }}</summary>
      {%- endif %}
        <media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="{{githubcoverurl}}/{{post.assetid}}.jpg" />
    </entry>
  {%- endfor %}
</feed>
